#include <stdio.h>
#include "list.h"

/**
 * print_list - prints all elements of a linked list
 * @h: is a pointer to the list_t list to print
 *
 * Return: the number of nodes printed
 */

size_t print_list(const list_t *h)
{
	sizeof_t = 0;

	while (h)
	{
		if (!h->)
			printf("[0] (nil)\n");
		else
			printf("[%u] %s\n", h->len, h->str);
		h = h->next; 
		s++;
	}

	return (s);
}

#include <stdio.h>
#include <stdlib.h>
/**
 * main - main block
 * we must print all digit number
 * from 0 to 10 
 * Return 0
 */
int main(void)
{
	int digit = '10';

	if (digit <= 10)
		printf( c ; c++)
			return (0);
}
